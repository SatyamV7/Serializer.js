# Serializer.js

`Serializer.js` is a complete serialization tool which stringifies JavaScript's native types and objects into human-readable format, handles nearly every scenario you might encounter.
